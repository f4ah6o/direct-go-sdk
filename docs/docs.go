// Package docs hosts compile-checked README example code. The doccheck tool
// (tools/doccheck) materializes fenced ```go blocks from the repository's
// README files under gen/ (gitignored) and builds them here, where replace
// directives resolve the local modules.
package docs
